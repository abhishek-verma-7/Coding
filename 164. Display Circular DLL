vector<int> displayList(Node *head)
{
    vector<int> list;
    Node *p = head;
    
    while(p->next != head)
    {
        list.push_back(p->data);
        p = p->next;
    }
    list.push_back(p->data);
    
    return list;
}
