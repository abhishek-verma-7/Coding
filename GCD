int gcd(int a, int b){

        
    //code here to calculate and return gcd of a and b
    int small= min(a,b);
    int ans=1;
    for(int i=1; i<=small; i++)
    {
        if(a%i==0 && b%i==0)
            ans = i;
    }
    return ans;
}
