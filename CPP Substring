string substring(string S, int L, int R) {
        // code here
        string str = "";
        for(int i=0; i<=R; i++)
        {
            if(i>=L)
                str += S[i];
        }
        
        return str;
    }
