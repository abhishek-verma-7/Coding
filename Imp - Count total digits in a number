int countDigits(int n)
    {
       //Your code here
       // if n is less than 10 then cout is 1
       if(n < 10)
            return 1;
            
        else 
            // recursively caount the digits of n
            return 1 + countDigits(n/10);
    }
